---
import Layout from "../layouts/Layout.astro";
import GraphVisualizer from "../components/GraphVisualizer";
import unminimize from "../../public/angle.svg";
import Dijkstra from "../components/Algos/Dijkstra";
---

<Layout title="Graph Visualizer">
  <main>
    <div class="gui">
      <button class="icon_btn unminimize">
        <img src={unminimize.src} class="unminimize_icon" />
      </button>
      <section class="gui_input graph_input">
        <div class="btn_panel">
          <button class="btn minimize">
            <img src={unminimize.src} alt="" class="minimize_icon" />
          </button>
          <button class="btn open_algos">Algos</button>
          <button class="btn open_perf_sett">Settings</button>
        </div>
        <form class="make_graph">
          <label for="vertices">Input vertices</label>
          <input
            type="text"
            id="vertices"
            name="vertices"
            placeholder="ex. A,B,C..."
          />
          <label for="edges">Input edges</label>
          <input
            type="text"
            id="edges"
            name="edges"
            placeholder="ex. A,B,4 C,A,5..."
          />
          <label for="isOriented" id="isOriented_label" class="checkbox_label"
            >Oriented: <input
              type="checkbox"
              name="isOriented"
              id="isOriented"
              class="checkbox_input"
            /></label
          >
          <button type="submit" class="btn">Make</button>
          <h4 class="error"></h4>
        </form>
      </section>
      <section class="gui_input perf_settings">
        <label for="smooth_render" class="checkbox_label"
          >Smooth render: <input
            type="checkbox"
            name="smooth_render"
            id="smooth_render"
            class="checkbox_input"
          /></label
        >
        <!-- <label for="edge_color"
          >Edge Color <br /><span class="tip">not functional yet</span></label
        >
        <input type="color" name="edge_color" id="" /> -->
      </section>
      <section class="gui_input algos">
        <form action="" class="algos_dijkstra">
          <label for="">Dijkstra</label>
          <input
            type="text"
            id="dijkstra_input"
            name="dijkstra_input"
            class="dijkstra_input"
          />
          <button class="btn" type="submit">Start</button>
        </form>
      </section>
    </div>
    <GraphVisualizer client:load />
    <Dijkstra client:load />
  </main>
</Layout>

<script src="../scripts/graph_input.ts"></script>
<script src="../scripts/perf_settings.ts"></script>
<script src="../scripts/gui_controller.ts"></script>
<script src="../scripts/algos_controller.ts"></script>

<style>
  main {
    width: 100%;
    height: 100%;
    /* max-width: calc(100% - 2rem); */
    color: white;
    font-size: 20px;
    line-height: 1.6;
  }
  .astro-a {
    position: absolute;
    top: -32px;
    left: 50%;
    transform: translatex(-50%);
    width: 220px;
    height: auto;
    z-index: -1;
  }
  h1 {
    font-size: 4rem;
    font-weight: 700;
    line-height: 1;
    text-align: center;
    margin-bottom: 1em;
  }
  .btn {
    color: rgb(204, 204, 204);
    padding: 8px 12px;
    font-size: 1rem;
    font-weight: 800;
    font-family: "Semplicita Pro";
    background-color: rgba(var(--color), 100%);
    border-radius: 10px;
    border: none;
    /* border: 1px solid rgba(var(--accent)); */
    transition: 0.1s;
  }
  .btn:hover {
    background-color: rgba(var(--color), 70%);
  }
  input {
    accent-color: rgba(var(--color), 70%);
    border: 2px solid white;
    border-radius: 0.25rem;
    background-color: rgb(245, 239, 255);
    font-weight: 500;
    height: 1.5rem;
    margin: 0.5rem 0;
  }
  input:checked {
    background-color: black;
  }
  input:focus {
    background-color: rgb(245, 240, 255);
  }
  .btn_panel {
    display: flex;
    justify-content: space-between;
    align-items: center;
  }
  .icon_btn {
    /* width: 2rem; */
    background: none;
    margin: 1.5rem;
    border: none;
    position: absolute;
  }
  .unminimize {
    display: none;
  }
  .unminimize_icon {
    width: 2rem;
    color: white;
    padding: 4px 4px;
    border: 1px rgba(var(--color), 100%) solid;
    border-radius: 10px;
    /* display: none; */
    /* height: 2rem; */
  }
  .minimize {
    padding: 0 4px;
  }
  .minimize_icon {
    width: 2rem;
    color: white;
    transform: rotate(180deg);
  }
  .gui {
    display: flex;
    flex-flow: row wrap;
    align-items: start;
    margin: 2rem;
    position: absolute;
    z-index: 2;
  }
  .gui_input {
    display: flex;
    flex-direction: column;
    /* width: 40%; */
    max-width: 200px;
    background-color: rgba(var(--accent-dark), 33%);
    border-radius: 10px;
    padding: 1rem;
    margin: 0.2rem;
  }
  .graph_input {
  }
  .open_perf_sett {
  }
  .perf_settings {
    display: none;
    /* margin: 0 0.5rem; */
  }
  .make_graph {
    display: flex;
    flex-direction: column;
  }
  .make_graph > label,
  .make_graph > input {
    font-size: 1rem;
  }
  .tip {
    color: rgb(130, 130, 130);
  }
  .checkbox_label {
    display: flex;
    font-size: 1rem;
  }
  .checkbox_input {
    margin: 3px 3px 0 3px;
  }
  .error {
    color: red;
    margin: 1rem 0 0 0;
  }
  .vertice {
    width: 2rem;
    height: 2rem;
    background-color: blanchedalmond;
  }
  .text-gradient {
    background-image: var(--accent-gradient);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-size: 400%;
    background-position: 0%;
  }
</style>
